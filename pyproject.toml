[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "autocache"
version = "0.1.0"
description = "A lightweight Python caching library"
requires-python = ">=3.9"
license = { file = "LICENSE" }